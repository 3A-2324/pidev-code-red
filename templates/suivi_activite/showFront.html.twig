{% extends 'basef.html.twig'%}

{% block title %}Suivi Activite{% endblock %}

{% block body %}

        <h1>SuiviActivité</h1>

        <table class="table">
            <tbody>
            <tr>
                <th>Id</th>
                <td>{{ suivi_activit_.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ suivi_activit_.date ? suivi_activit_.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Rep</th>
                <td>{{ suivi_activit_.rep }}</td>

            </tr>
            <tr>
                <th>User</th>
                <td>{{ suivi_activit_.user.Nom }}</td>

            </tr>
            <tr>
                <th>Activite</th>
                <td>{{ suivi_activit_.activite.nom }}</td>

            </tr>
            <tr>
                <th>Photo Activite</th>
                <td><img src="{{ asset('/img/' ~ suivi_activit_.activite.image) }}" style="width: 50px"height="50px"></td>

            </tr>
            </tbody>
        </table>

        <a href="{{ path('app_suivi_activit__index_front') }}">back to list</a>

        <a href="{{ path('app_suivi_activit__edit_front', {'id': suivi_activit_.id}) }}">edit</a>

        {{ include('suivi_activite/_delete_form.html.twig') }}
{% endblock %}
