{% extends 'basef.html.twig' %}

{% block title %}
    Nouveau Panier
{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1 class="mb-4">Nouveau Panier</h1>

        <div class="row">
            <div class="col-md-6">
                {{ include('panier/_form.html.twig') }}
            </div>
        </div>

        <a href="{{ path('app_panier_index') }}" class="btn btn-secondary mt-3">Retour à la liste</a>
    </div>
{% endblock %}


