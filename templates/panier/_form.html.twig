{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}

<div class="form-group row">
    <label for="Date_ajout" class="col-sm-2 col-form-label">Date Ajout</label>
    <div class="col-sm-10">
        {{ form_widget(form.Date_ajout, {'attr': {'class': 'form-control', 'placeholder': 'Date Ajout'}}) }}
    </div>
</div>

<div class="form-group row">
    <label for="quantite" class="col-sm-2 col-form-label">Quantité</label>
    <div class="col-sm-10">
        {{ form_widget(form.quantite, {'attr': {'class': 'form-control', 'placeholder': 'Quantité'}}) }}
    </div>
</div>



<div class="form-group row">
    <div class="col-sm-10 offset-sm-2">
        <button type="submit" class="btn btn-primary">{{ button_label|default('Enregistrer') }}</button>
    </div>
</div>

{{ form_end(form) }}

